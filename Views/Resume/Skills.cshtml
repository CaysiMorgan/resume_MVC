
@model 
@{
    ViewBag.Title = "Skills";
}
<h2>Resume Maker</h2>
<div>
    <fieldset>
        <legend>Personal Information</legend>
        @using (Html.BeginForm())
        {
            <div>
                <div class="editor-label ">
                    @Html.LabelFor(p => p.Skills)
                </div>
                <div class="editor-field">
                    @Html.TextBoxFor(p => p.Skills)
                    @Html.ValidationMessageFor(p => p.Skills)
                </div>
            </div>

